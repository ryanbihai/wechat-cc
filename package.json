{
  "name": "wechat-cc",
  "version": "0.5.0",
  "description": "WeChat Channel for Claude Code via OceanBus L0 P2P — OB + Monitor, no --channels",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target bun && bun build src/agent.ts --outfile dist/agent.js --target node",
    "start": "bun install --no-summary && bun dist/index.js",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "oceanbus": "latest",
    "qrcode-terminal": "^0.12.0",
    "weixin-bot-plugin": "^2.0.1",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3"
  }
}
